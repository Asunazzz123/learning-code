# cuDF Backend Placeholder

Reserved for RAPIDS/cuDF string and regex baselines. Start by checking whether the tokenizer
pretokenization regex can be represented by cuDF's regex feature set; if not, benchmark a
simplified regex and document the semantic gap.
