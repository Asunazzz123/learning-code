# Data

Place local corpora here or pass `--input-path` to the benchmark CLI.

Large text files should stay out of git. A useful first sweep is to test short prompts, 4K-16K
character contexts, and larger batch-like corpora separately.
